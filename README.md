# p5-ts-starter

A starter project that's setup for a single p5.js sketch written in TypeScript.

## Get Started

First, get the code:

```bash
git clone https://github.com/ProjectDepot/p5-ts-starter
cd p5-ts-starter
npm install
```

Now you can start the server and watch the code for changes:

```bash
npm run dev
```
